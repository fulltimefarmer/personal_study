"""
考点：数组、哈希表、矩阵
题目：Valid Sudoku（有效的数独）
思路：三组哈希集合分别记录每行、每列、每个 3x3 宫内已出现的数字，遍历时检查是否有重复
时间复杂度：O(1)，固定 9x9 棋盘
空间复杂度：O(1)，固定大小
"""
from typing import List

def isValidSudoku(board: List[List[str]]) -> bool:
    # 使用 Python set（集合）存储已出现的数字
    # set.add() 添加元素，set 自动去重，in 操作 O(1)
    # 列表推导式：创建 9 个空 set
    rows: list[set[str]] = [set() for _ in range(9)]
    cols: list[set[str]] = [set() for _ in range(9)]
    boxes: list[set[str]] = [set() for _ in range(9)]

    for i in range(9):
        for j in range(9):
            num = board[i][j]
            if num == ".":
                continue  # 跳过空格

            # 计算当前格子属于第几个 3x3 宫
            # i // 3 得到行分组（0、1、2），j // 3 得到列分组（0、1、2）
            # box_index = 行分组 × 3 + 列分组，将二维宫内编号转换为一维索引
            box_index = (i // 3) * 3 + (j // 3)

            # 检查重复：如果当前数字已存在于对应的行、列或宫中
            if num in rows[i] or num in cols[j] or num in boxes[box_index]:
                return False

            # 将数字添加到对应的行、列、宫集合中
            rows[i].add(num)
            cols[j].add(num)
            boxes[box_index].add(num)

    return True

if __name__ == "__main__":
    board1 = [
        ["5", "3", ".", ".", "7", ".", ".", ".", "."],
        ["6", ".", ".", "1", "9", "5", ".", ".", "."],
        [".", "9", "8", ".", ".", ".", ".", "6", "."],
        ["8", ".", ".", ".", "6", ".", ".", ".", "3"],
        ["4", ".", ".", "8", ".", "3", ".", ".", "1"],
        ["7", ".", ".", ".", "2", ".", ".", ".", "6"],
        [".", "6", ".", ".", ".", ".", "2", "8", "."],
        [".", ".", ".", "4", "1", "9", ".", ".", "5"],
        [".", ".", ".", ".", "8", ".", ".", "7", "9"],
    ]
    assert isValidSudoku(board1) == True

    board2 = [
        ["8", "3", ".", ".", "7", ".", ".", ".", "."],
        ["6", ".", ".", "1", "9", "5", ".", ".", "."],
        [".", "9", "8", ".", ".", ".", ".", "6", "."],
        ["8", ".", ".", ".", "6", ".", ".", ".", "3"],
        ["4", ".", ".", "8", ".", "3", ".", ".", "1"],
        ["7", ".", ".", ".", "2", ".", ".", ".", "6"],
        [".", "6", ".", ".", ".", ".", "2", "8", "."],
        [".", ".", ".", "4", "1", "9", ".", ".", "5"],
        [".", ".", ".", ".", "8", ".", ".", "7", "9"],
    ]
    assert isValidSudoku(board2) == False
    print("全部通过 ✓")
